/** Next.js Automatically Will Redirect to this page
 *  if not match found while redirecting
 */

import { NotFoundPage } from "@/views";

export default function NotFound() {
  return <NotFoundPage />;
}
